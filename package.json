{
  "name": "perf-sentinel-hub-web",
  "private": true,
  "description": "Type-checking for the launcher's JSDoc. The shipped app has no build step.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/launcher.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
