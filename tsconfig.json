{
  "//": "Type-checks the launcher's JSDoc. wwwroot/ ships as plain JavaScript with no build step, so this emits nothing and only reads. app.js is out of scope on purpose: it carries three annotations against launcher.js's 122, so checking it would report noise rather than defects.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "target": "ES2020",
    "lib": ["ES2020", "DOM"],
    "moduleResolution": "node",
    "strict": false
  },
  "files": ["PerfSentinelHub/wwwroot/launcher.js"]
}
