# PerfSentinelHub lab validation ledger
# format: <version>\t<lab_commit_sha>\t<YYYY-MM-DD>\t<PASS|FAIL>
# Append-only. Lines are produced by `scripts/record-validation.sh vX.Y.Z PASS` in the perf-sentinel-simulation-lab repo.
#
# The lab exercises the Hub through six scenarios: hub-ingestion, hub-derived-status,
# hub-lineage-mutation, hub-retention-purge, hub-source-reachability and
# hub-plugin-contract. They run a real Hub image against a real daemon, so a
# validation names the Hub commit it was built from, not only the version.
#
# The first entry validates a version that has never been published: the whole
# point of the gate is to assert the lab saw the build before the tag exists.
v0.1.0	d7f5889	2026-08-29	PASS
# v0.1.2 PASS covers the scope feature/launcher-sanitizer-knobs touched, on Hub
# commit 9e30921: all six hub-* scenarios green against a daemon on the published
# 0.18.0 image, plus the launcher's own probe, which the lab had never exercised
# because no manifest configured Hub:Analysis:EngineBinaryPath. With it set, the
# Hub reads engine 0.18.0 and serves all ten detection knobs, sanitizer_aware_min_cv
# included. A launcher run end to end is still uncovered: the lab configures no
# trace-backend source for the Hub, and tests/launcher-e2e.test.js covers that path
# against the real binary.
v0.1.2	0e57121	2026-09-02	PASS
# v0.1.3 carries the same lab evidence as the v0.1.2 line above, on the same lab
# commit: the six hub-* scenarios ran against Hub image 9e30921, which is this tag
# minus three CSS fixes and a changelog entry. No hub-* scenario reads the
# stylesheet, so the lab cannot tell the two builds apart, and the version in
# column one has to be the one about to be tagged for the gate to find it.
v0.1.3	0e57121	2026-09-02	PASS
# v0.1.6 targeted pass rather than the six hub-* scenarios: hub-incidents-mirror
# end to end, 13 verdicts over six legs, which is the scope this tag adds. It ran
# against Hub image 0.1.6-2cef45f, this tag minus the engine repin to perf-sentinel
# 0.20.1 that landed after the run. That repin changes the binary a backend analysis
# shells out to, not the incident poll, the mirror or the filters the scenario
# exercises, so the lab cannot tell the two builds apart. The daemon side ran a
# local 0.20.1 build, the run having also served to prove its archive permission fix
# on a real fsGroup volume.
v0.1.6	dadd839	2026-09-05	PASS
# v0.1.7 carries the v0.1.6 lab evidence above, on the same lab commit, and
# runs no pass of its own. Everything this tag adds is presentation or
# tooling: the finding type reads as a label, the severity and the kind chips
# take the engine's tones, two CSS spacing fixes on the incidents screen, a
# types.d.ts with the typecheck its header had promised, and the CI job that
# runs it. No route, no schema, no stored value moves. No hub-* scenario reads
# the launcher's rendering: they assert API payloads and stored rows, which is
# why the lab cannot tell the two builds apart. Worth naming rather than
# leaving to inference: the v0.1.6 line it leans on is itself a targeted
# single-scenario pass, hub-incidents-mirror, and the newest full six-scenario
# hub-* run in this ledger is still the v0.1.2 and v0.1.3 one on lab commit
# 0e57121.
v0.1.7	dadd839	2026-09-06	PASS
# v0.1.8 is the first full six-scenario hub-* run in this ledger since v0.1.2
# and v0.1.3: hub-ingestion, hub-derived-status, hub-lineage-mutation,
# hub-retention-purge, hub-plugin-contract and hub-source-reachability all
# green, plus hub-incidents-mirror, inside the perf-sentinel v0.21.0 lab pass
# on lab commit 070998a. The Hub was built by seed-hub-local from ddd4093,
# this tag's parent, and polled a daemon running the 0.21.0 engine, which is
# what the tag is about: it repins the embedded engine from 0.20.2 to 0.21.0.
# Two reservations, since the run predates the tag. The image the scenarios
# exercised still carried the 0.20.2 engine binary, so what is proven is the
# Hub against a 0.21.0 daemon, not the 0.21.0 binary run as a subprocess by a
# backend analysis. And the three hub-* scenarios that build an isolated pair
# failed first on ErrImageNeverPull, seed-hub-local not having been run yet,
# then passed on rerun once it had, which is a lab prerequisite rather than a
# Hub defect.
v0.1.8	070998a	2026-09-10	PASS
